#include<stdio.h>
int strlength(char array[]){
    int c=0;
    for(int i=0;array[i]!='\0';i++){
        c++;
    }
    return c;
}
int main(){
   char* name="RAUSHAN";
   int x = strlength(name);
   printf("%d",x);
    return 0;
}
