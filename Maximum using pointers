#include <stdio.h>
int max(int *a,int* b){
    if(*a>*b){
        return *a ;
    }
    else {
        return *b ;
    }
}
int main(void) {
   printf("Enter two numbers : ");
   int x,y;
   scanf(" %d%d",&x,&y);
   printf("Maxmimum number is : %d",max(&x,&y));

    return 0;
}
